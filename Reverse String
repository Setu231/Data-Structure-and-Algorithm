class Solution {
    public void reverseString(char[] s) {
        int n = s.length - 1;
        for (int i = 0; i < s.length/2; i++, n--) {
            char temp = s[n];
            s[n] = s[i];
            s[i] = temp;
        }
    }
}