class Solution {
    public int arrayPairSum(int[] nums) {
        int k = 0;
        Arrays.sort(nums);
        for(int i = 0; i < nums.length -1; i = i+2) {
            k += nums[i];
        }
        return k;
    }
}